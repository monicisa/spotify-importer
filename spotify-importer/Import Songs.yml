info:
  name: Import Songs
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:7071/api/importSongs
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​